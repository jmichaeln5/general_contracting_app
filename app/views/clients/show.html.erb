<div class="card container">

<div class="display-4 text-center py-5"> <%= @client.name %> </div>

<% if @client.description.present? %>
  <div class="text-center">
    <strong>Description:</strong>
    <%= @client.description %>
  </div>
  <br>
<% end %>

<hr>

<div class="text-center">
<%= link_to 'Edit Client', edit_client_path(@client), class:"btn btn-outline-secondary" %>
  <br><br>
    <div class="card container">
    <br>

    <% if @client.link.present? %>
      <strong>Address:</strong>
      <a href="<%= @client.link %>" target="_blank"><%= @client.full_street_address %></a>

    <% else %>
      <strong>Address:</strong>
      <%= @client.full_street_address %></a>
    <% end %>

    <br><br>
    <div id="map"></div>
    <br>
    </div>

    <br><br>
  </div>
</div>
<br>
<div class="text-center">
  <%= link_to 'Back', :back, class:"btn btn-primary", style: 'color:#FFFFFF;' %>
</div>


<script type="text/javascript">

var lat = <%= @client.lat %>;
var lng = <%= @client.lng %>;

// Initialize and add the map
function initMap() {
  // The location of Uluru
  var uluru = {lat, lng};
  // The map, centered at Uluru
  var map = new google.maps.Map(
      document.getElementById('map'), {zoom: 14, center: uluru});
  // The marker, positioned at Uluru
  var marker = new google.maps.Marker({position: uluru, map: map});
}

</script>

<script async defer
src="https://maps.googleapis.com/maps/api/js?key=AIzaSyDUrlQaMPYAMf2W6vaHFF7ufv-Bp6A7dZo&callback=initMap">
</script>
